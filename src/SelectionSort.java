public class SelectionSort {
    public static void main(String[] args) {
        int[] unsortedArray = {5, 2, 6, 0, 80, 60, 1, 40};

        for(int i = 1; i < unsortedArray.length -1; i++){
            if(unsortedArray[i] < unsortedArray[i - 1]){

            }
        }




    }

    private static void swap(int[] unsortedArrayint, int first, int second){
        int temp = first;

        unsortedArrayint[first] = unsortedArrayint[second];

    }
}
